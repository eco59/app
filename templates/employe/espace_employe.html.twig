{% extends 'base.html.twig' %}

{% block title %}Espace employe{% endblock %}

{% block body %}
<div class="pt-4 min-vh">
    <div>
        <h1 class="mb-4 font-weight-bold text-center">Bienvenue sur l'espace employe</h1>
    </div>
    <div class="d-flex justify-content-center pt-4">
        <div class="button-grid">
            <a href="{{ path('app_mes_favoris') }}" class="custom-btn bg-blue">Mes inscriptions</a>
            <a href="{{ path('app_ajout_event') }}" class="custom-btn bg-blue">Créer un évènement</a>
            <a href="{{ path('app_mes_events') }}" class="custom-btn btn-events bg-pink">Mes événements</a>
            <a href="{{ path('app_event_approved') }}" class="custom-btn btn-events bg-pink mb-3">Evenement approuvé</a>
            <a href="{{ path('event_pending_list') }}" class="custom-btn bg-blue me-5">event a venir</a>
            <a href="{{ path('app_historique_score') }}" class="custom-btn bg-color">Historique des scores</a>

            <a href="{{ path('app_logout') }}" class="custom-btn btn-events bg-color">Se déconnecter</a>
        </div>
    </div>
</div>
{% endblock %}